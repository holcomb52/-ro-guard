[client]
# "auto" = Share/admin toolbar only for Streamlit Cloud workspace admins (you).
# Regular RO Guard users do not see Share, Deploy, or developer menu items.
toolbarMode = "auto"

[browser]
gatherUsageStats = false
